// const constructLivePlays = require('../constructLivePlays');

describe('Test the constructLivePlays', () => {
  test('It should ...', () => {
    expect(1).toBe(1);
  });
});
// Honestly don't need to worry about internals much,
// just foxus on passing certain counts and making sure
// certain events are present
